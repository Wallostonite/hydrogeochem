# Minimal set for running the test suite and the API locally.
# Deliberately excludes phreeqpy, psycopg and celery: none of them are needed to
# exercise the chemistry, the orchestration, or the HTTP layer.
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
tenacity>=9.0
prometheus-client>=0.20
fastapi>=0.115
uvicorn[standard]>=0.30
pytest>=8
pytest-cov
requests>=2.32
